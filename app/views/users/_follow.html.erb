<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div><!--this sets the other user's id to 
  followed_id not sure how it is determining that from @user.id-->
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>